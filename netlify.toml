[build]
  command = "cd client && CI=false npm install && CI=false npm run build"
  publish = "client/build"

[build.environment]
  CI = "false"
  GENERATE_SOURCEMAP = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
